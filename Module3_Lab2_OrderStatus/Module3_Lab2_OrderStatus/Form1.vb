Public Class Form1
    Private Function GetInStock()
        'Displays an input box asking the user To enter the number Of spools In stock. 
        'The Function() should Return the value entered by the user.

    End Function
    Private Function ReadyToShip()
        'Accepts the following arguments: the number Of spools In stock And the number Of spools ordered. 
        'The Function returns the number Of spools ready To ship.

    End Function
    Private Function BackOrdered()
        'Accepts the following arguments: the number Of spools In stock And the number Of spools ordered. 
        'The Function returns the number Of spools On back order. If no spools are On back order, it returns 0.

    End Function
    Private Function ShippingCharges()
        'Accepts the following arguments: the number Of spools ready To ship And the per-spool shipping charges. 
        'The Function returns the total shipping And handling charges.

    End Function
    Private Sub ResetSpools()
        'Clears the text box And the check box.

    End Sub
    Private Sub ResetDelivery()
        'Clears the labels that display the delivery information.

    End Sub
End Class
